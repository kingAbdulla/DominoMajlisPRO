# Domino Majlis PRO Engineering Manual v1.0

Place the contents of this package in the repository root.

Required root layout:

```text
.github/copilot-instructions.md
docs/00_READ_FIRST.md
docs/*.md
```

After copying:

```bash
git add docs .github README_ENGINEERING_DOCS.md
git commit -m "Add official engineering manual v1"
git push
```

Then tell any AI agent:

```text
Read /docs/00_READ_FIRST.md and .github/copilot-instructions.md before answering.
Treat them as the official engineering constitution.
```
