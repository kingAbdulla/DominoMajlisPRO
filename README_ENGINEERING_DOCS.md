# Domino Majlis PRO Engineering Constitution

Place this package at the root of the `DominoMajlisPRO` repository.

It creates:
- `/docs` engineering documentation
- `/.github/copilot-instructions.md`

Any AI working on the project must start with `/docs/00_READ_FIRST.md`.
