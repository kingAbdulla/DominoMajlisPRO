const API_BASE=location.origin;
const app=document.getElementById('app');
const storage={
 get session(){try{return JSON.parse(localStorage.getItem('dmp_prod_session')||'null')}catch{return null}},
 set session(v){v?localStorage.setItem('dmp_prod_session',JSON.stringify(v)):localStorage.removeItem('dmp_prod_session')},
 get deviceId(){let v=localStorage.getItem('dmp_prod_device_id');if(!v){v=`WEB-${crypto.randomUUID()}`;localStorage.setItem('dmp_prod_device_id',v)}return v},
 get activeMatch(){try{return JSON.parse(localStorage.getItem('dmp_prod_active_match')||'null')}catch{return null}},
 set activeMatch(v){v?localStorage.setItem('dmp_prod_active_match',JSON.stringify(v)):localStorage.removeItem('dmp_prod_active_match')}
};
const state={session:storage.session,route:'home',teams:[],players:[],matches:[],rankings:[],online:false,activeMatch:storage.activeMatch};
const headers=()=>state.session?.accessToken?{'Authorization':`Bearer ${state.session.accessToken}`,'X-Device-Id':storage.deviceId}:{};
async function api(path,options={}){const r=await fetch(`${API_BASE}${path}`,{...options,headers:{'Content-Type':'application/json',...headers(),...(options.headers||{})}});if(r.status===401){storage.session=null;state.session=null;renderAuth('انتهت الجلسة. سجّل الدخول مجددًا.');throw new Error('unauthorized')}const p=r.status===204?null:await r.json().catch(()=>null);if(!r.ok)throw new Error(p?.message||'تعذر تنفيذ العملية.');state.online=true;return p}
const escapeHtml=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const guid=()=>crypto.randomUUID();const nowIso=()=>new Date().toISOString();const payload=r=>r?.payload??r;
function rankFromXp(xp=0){if(xp>=5000)return'Majlis Legend';if(xp>=3500)return'Majlis Master';if(xp>=2500)return'Diamond';if(xp>=1700)return'Platinum';if(xp>=1000)return'Gold';if(xp>=500)return'Silver';if(xp>=150)return'Bronze';return'Unranked'}
function renderAuth(error=''){app.innerHTML=`<section class="boot-screen" style="display:block;min-height:auto;padding-top:6vh"><div class="brand-mark">♛</div><h1>Domino Majlis PRO</h1><p>الحساب السحابي الرسمي</p><div class="card" style="max-width:520px;margin:28px auto;text-align:right"><div class="auth-tabs"><button class="primary" id="loginMode">تسجيل الدخول</button><button class="secondary" id="registerMode">إنشاء حساب</button></div><label>اسم المستخدم</label><input class="field" id="username" autocomplete="username"><label class="field-label">كلمة المرور</label><input class="field" id="password" type="password" autocomplete="current-password"><button class="primary" id="submitAuth">متابعة</button><p class="message" id="authMessage">${escapeHtml(error)}</p></div></section>`;let mode='login';const login=document.getElementById('loginMode'),register=document.getElementById('registerMode');login.onclick=()=>{mode='login';login.className='primary';register.className='secondary'};register.onclick=()=>{mode='register';register.className='primary';login.className='secondary'};document.getElementById('submitAuth').onclick=async()=>{const username=document.getElementById('username').value.trim(),password=document.getElementById('password').value,msg=document.getElementById('authMessage');if(username.length<3||password.length<8){msg.textContent='اسم المستخدم 3 أحرف على الأقل وكلمة المرور 8 أحرف على الأقل.';return}msg.textContent='جارٍ الاتصال...';try{const s=await api(`/api/preview/${mode}`,{method:'POST',body:JSON.stringify({username,password,deviceId:storage.deviceId})});storage.session=s;state.session=s;await bootAuthenticated()}catch(e){if(e.message!=='unauthorized')msg.textContent=e.message}}}
async function health(){try{state.online=(await fetch(`${API_BASE}/api/health`)).ok}catch{state.online=false}}
async function loadResource(name){return (await api(`/api/v1/${name}?includeDeleted=false`).catch(()=>[])).map(payload).filter(Boolean)}
async function loadTeams(){const basic=await api('/api/preview/me/teams').catch(()=>[]);const rich=await loadResource('teams');const map=new Map(rich.map(t=>[t.teamId,t]));state.teams=basic.map(t=>({...map.get(t.teamId),teamId:t.teamId,teamName:map.get(t.teamId)?.teamName||t.name,name:t.name}));for(const t of rich)if(!state.teams.some(x=>x.teamId===t.teamId))state.teams.push({...t,name:t.teamName})}
async function loadPlayers(){state.players=await loadResource('players')}
async function loadMatches(){state.matches=(await loadResource('matches')).sort((a,b)=>new Date(b.lastPlayedTime||b.matchDate)-new Date(a.lastPlayedTime||a.matchDate))}
async function loadRankings(){state.rankings=await loadResource('rankings')}
async function bootAuthenticated(){await Promise.allSettled([loadTeams(),loadPlayers(),loadMatches(),loadRankings(),health()]);renderShell()}
function topBar(){const u=state.session?.user||{},p=state.players.find(x=>x.playerId===u.playerId),initial=(p?.playerName||u.displayName||'?')[0].toUpperCase();return `<header class="topbar"><div class="wallet-chip"><span>🪙</span><strong>0</strong><button>+</button></div><div class="wallet-chip"><span>💎</span><strong>0</strong><button>+</button></div><button class="avatar-button" data-route="profile"><span>${escapeHtml(initial)}</span><span class="level-badge">${p?.playerLevel||1}</span></button><button class="icon-button" data-route="settings">⚙</button></header>`}
const teamName=id=>state.teams.find(t=>t.teamId===id)?.teamName||state.teams.find(t=>t.teamId===id)?.name||id||'فريق';
const playerName=id=>state.players.find(p=>p.playerId===id)?.playerName||'';
const teamOptions=(s='')=>`<option value="">اختر فريقًا</option>${state.teams.map(t=>`<option value="${escapeHtml(t.teamId)}" ${t.teamId===s?'selected':''}>${escapeHtml(t.teamName||t.name)}</option>`).join('')}`;
const playerOptions=(s='')=>`<option value="">بدون لاعب</option>${state.players.map(p=>`<option value="${escapeHtml(p.playerId)}" ${p.playerId===s?'selected':''}>${escapeHtml(p.playerName)}</option>`).join('')}`;
function teamCard(t){return `<article class="list-item"><div class="emblem" style="border-color:${escapeHtml(t.colorHex||'#8d6824')}">${escapeHtml(t.emblem||'♜')}</div><div><b>${escapeHtml(t.teamName||t.name)}</b><br><small>${escapeHtml(playerName(t.player1Id)||t.player1||'—')} / ${escapeHtml(playerName(t.player2Id)||t.player2||'—')}</small><br><small>${escapeHtml(t.rank||'Unranked')} · ${t.xp||0} XP</small></div><button class="mini-button" data-edit-team="${escapeHtml(t.teamId)}">تعديل</button></article>`}
function playerCard(p){return `<article class="list-item"><div class="emblem">${escapeHtml((p.playerName||'?')[0])}</div><div><b>${escapeHtml(p.playerName)}</b><br><small>${escapeHtml(p.playerRank||'Unranked')} · المستوى ${p.playerLevel||1}</small><br><small>${p.wins||0} فوز · ${p.losses||0} خسارة · ثقة ${p.trustScore??100}%</small></div><button class="mini-button" data-edit-player="${escapeHtml(p.playerId)}">تعديل</button></article>`}
const routes={
 home:()=>`<h1 class="page-title">الرئيسية</h1><p class="page-subtitle">مجلسك، فرقك، ولاعبوك ومبارياتك.</p><section class="card hero"><h2>مرحبًا ${escapeHtml(state.session?.user?.displayName||'')}</h2><div class="metrics"><div class="metric"><b>${state.teams.length}</b><span>الفرق</span></div><div class="metric"><b>${state.players.length}</b><span>اللاعبون</span></div><div class="metric"><b>${state.matches.length}</b><span>المباريات</span></div></div></section><div class="section-grid"><button class="action-card" data-route="players"><h3>اللاعبون</h3><p>إنشاء الملفات وإدارة الإحصائيات.</p></button><button class="action-card" data-route="teams"><h3>الفرق</h3><p>التشكيلات والهوية والإحصائيات.</p></button><button class="action-card" data-route="matches"><h3>مباراة</h3><p>تسجيل الجولات والنتيجة.</p></button><button class="action-card" data-route="rankings"><h3>التصنيفات</h3><p>XP والرتب والمراكز.</p></button></div>`,
 players:()=>`<h1 class="page-title">اللاعبون</h1><p class="page-subtitle">ملفات اللاعبين المرتبطة بالحساب.</p><section class="card"><input id="playerId" type="hidden"><label>اسم اللاعب</label><input id="playerName" class="field" maxlength="40"><label class="field-label">النبذة</label><input id="playerBio" class="field" maxlength="160"><button id="savePlayer" class="primary">حفظ اللاعب</button><p id="playerMessage" class="message"></p></section><section class="card">${state.players.length?state.players.map(playerCard).join(''):'<p class="muted">لا يوجد لاعبون بعد.</p>'}</section>`,
 teams:()=>`<h1 class="page-title">الفرق</h1><p class="page-subtitle">إنشاء الفرق وتحديد تشكيلاتها.</p><section class="card"><input id="teamId" type="hidden"><label>اسم الفريق</label><input id="teamName" class="field"><div class="round-grid"><div><label class="field-label">اللاعب الأول</label><select id="player1" class="field">${playerOptions()}</select></div><div><label class="field-label">اللاعب الثاني</label><select id="player2" class="field">${playerOptions()}</select></div></div><button id="saveTeam" class="primary">حفظ الفريق</button><p id="teamMessage" class="message"></p></section><section class="card">${state.teams.length?state.teams.map(teamCard).join(''):'<p class="muted">لا توجد فرق بعد.</p>'}</section>`,
 matches:()=>state.activeMatch?matchBoard():matchSetup(),history:()=>historyView(),rankings:()=>rankingsView(),
 profile:()=>{const u=state.session?.user||{},p=state.players.find(x=>x.playerId===u.playerId)||state.players[0];return `<h1 class="page-title">الملف الشخصي</h1><section class="card profile-hero"><div class="avatar-button">${escapeHtml((p?.playerName||u.displayName||'?')[0])}</div><div><h2>${escapeHtml(p?.playerName||u.displayName||'')}</h2><p>${escapeHtml(p?.playerRank||'Unranked')} · المستوى ${p?.playerLevel||1}</p></div></section><section class="metrics"><div class="metric"><b>${p?.playerXP||0}</b><span>XP</span></div><div class="metric"><b>${p?.trustScore??100}%</b><span>الثقة</span></div><div class="metric"><b>${p?.wins||0}</b><span>الانتصارات</span></div></section>`},
 settings:()=>`<h1 class="page-title">الإعدادات</h1><section class="card"><div class="list-item"><div class="emblem">☁</div><div><b>حالة السحابة</b><br><small>${state.online?'متصل':'غير متصل'}</small></div><span>${state.online?'✓':'!'}</span></div><button id="logout" class="secondary">تسجيل الخروج</button></section>`
};
function matchSetup(){return `<h1 class="page-title">مباراة جديدة</h1><section class="card"><label>الفريق الأول</label><select id="team1" class="field">${teamOptions()}</select><label class="field-label">الفريق الثاني</label><select id="team2" class="field">${teamOptions()}</select><label class="field-label">القواعد</label><select id="rules" class="field"><option value="local">محلي</option><option value="international">دولي</option></select><label class="check-row"><input type="checkbox" id="ranked"> مباراة مصنفة</label><button id="startMatch" class="primary" ${state.teams.length<2?'disabled':''}>بدء المباراة</button><p id="matchMessage" class="message">${state.teams.length<2?'يجب إنشاء فريقين على الأقل.':''}</p></section>`}
function matchBoard(){const m=state.activeMatch;return `<h1 class="page-title">المباراة الجارية</h1><p class="page-subtitle">الجولة ${m.roundNumber} — ${m.isLocalRules?'محلي':'دولي'}</p><section class="card score-board"><div class="score-team"><span>${escapeHtml(m.team1Name)}</span><b>${m.team1Score}</b></div><div class="versus">VS</div><div class="score-team"><span>${escapeHtml(m.team2Name)}</span><b>${m.team2Score}</b></div></section><section class="card"><div class="round-grid"><div><label>${escapeHtml(m.team1Name)}</label><input id="round1" class="field" type="number" min="0" value="0"></div><div><label>${escapeHtml(m.team2Name)}</label><input id="round2" class="field" type="number" min="0" value="0"></div></div><button id="addRound" class="primary">حفظ الجولة</button><p id="roundMessage" class="message"></p></section><section class="card"><h2>سجل الجولات</h2>${roundHistory(m)}</section><div class="section-grid"><button id="saveExit" class="secondary">حفظ والخروج</button><button id="cancelMatch" class="secondary">إلغاء المباراة</button></div>`}
function roundHistory(m){return m.roundsHistory?.length?m.roundsHistory.map((r,i)=>`<div class="list-item"><div class="emblem">${i+1}</div><div><b>${r.team1RoundScore} - ${r.team2RoundScore}</b></div><span>${new Date(r.playedAt).toLocaleTimeString('ar-IQ',{hour:'2-digit',minute:'2-digit'})}</span></div>`).join(''):'<p class="muted">لم تسجل جولات بعد.</p>'}
function historyView(){return `<h1 class="page-title">سجل المباريات</h1><section class="card">${state.matches.length?state.matches.map(m=>`<article class="match-history"><div><b>${escapeHtml(m.team1Name)} <span>${m.team1Score}</span></b><small>ضد</small><b>${escapeHtml(m.team2Name)} <span>${m.team2Score}</span></b></div><div class="history-meta"><strong>${m.hasMeles?'ملص':m.isFinished?'منتهية':'غير مكتملة'}</strong><small>${new Date(m.matchDate||m.lastPlayedTime).toLocaleDateString('ar-IQ')}</small></div></article>`).join(''):'<p class="muted">لا توجد مباريات.</p>'}</section>`}
function rankingsView(){const teams=[...state.teams].sort((a,b)=>(b.xp||0)-(a.xp||0));return `<h1 class="page-title">التصنيفات</h1><p class="page-subtitle">ترتيب الفرق حسب XP الموسمي.</p><section class="card ranking-list">${teams.length?teams.map((t,i)=>`<article class="ranking-row"><strong>#${i+1}</strong><div><b>${escapeHtml(t.teamName||t.name)}</b><small>${escapeHtml(t.rank||rankFromXp(t.xp||0))} · ثقة ${t.trustScore??100}%</small></div><span>${t.xp||0} XP</span></article>`).join(''):'<p class="muted">لا توجد فرق.</p>'}</section>`}
function evaluateWinner(m){const max=Math.max(m.team1Score,m.team2Score),min=Math.min(m.team1Score,m.team2Score);if(m.team1Score===m.team2Score)return null;const winnerId=m.team1Score>m.team2Score?m.team1Id:m.team2Id,winnerName=m.team1Score>m.team2Score?m.team1Name:m.team2Name;if(m.isLocalRules&&max>=101&&min<25)return{winnerId,winnerName,hasMeles:true};if(max>=151)return{winnerId,winnerName,hasMeles:false};return null}
async function persist(resource,id,data){await api(`/api/v1/${resource}/${encodeURIComponent(id)}`,{method:'PUT',body:JSON.stringify({payload:data})})}
async function savePlayer(){const id=document.getElementById('playerId').value||`PLY-${guid().replaceAll('-','').toUpperCase()}`,name=document.getElementById('playerName').value.trim(),msg=document.getElementById('playerMessage');if(!name){msg.textContent='اسم اللاعب مطلوب.';return}const old=state.players.find(p=>p.playerId===id)||{};const p={playerId:id,playerName:name,bio:document.getElementById('playerBio').value.trim(),profileStatus:old.profileStatus||'Normal',playerRank:old.playerRank||'Unranked',playerXP:old.playerXP||0,playerLevel:old.playerLevel||1,seasonXP:old.seasonXP||0,lifetimeXP:old.lifetimeXP||0,trustScore:old.trustScore??100,totalMatches:old.totalMatches||0,wins:old.wins||0,losses:old.losses||0,highestScore:old.highestScore||0,winRate:old.winRate||0,bestWinStreak:old.bestWinStreak||0,currentWinStreak:old.currentWinStreak||0,createdAt:old.createdAt||nowIso(),lastActiveAt:nowIso(),lastUpdatedAt:nowIso(),avatarImage:old.avatarImage||'player_card.png'};await persist('players',id,p);await loadPlayers();renderShell()}
async function saveTeam(){let id=document.getElementById('teamId').value,name=document.getElementById('teamName').value.trim(),msg=document.getElementById('teamMessage');if(!name){msg.textContent='اسم الفريق مطلوب.';return}let old=state.teams.find(t=>t.teamId===id)||{};if(!id){const basic=await api('/api/preview/me/teams',{method:'POST',body:JSON.stringify({name})});id=basic.teamId;old={}}const p1=document.getElementById('player1').value,p2=document.getElementById('player2').value;if(p1&&p1===p2){msg.textContent='لا يمكن اختيار اللاعب نفسه مرتين.';return}const t={...old,teamId:id,teamName:name,name,player1Id:p1,player2Id:p2,player1:playerName(p1),player2:playerName(p2),isSinglePlayer:!p2,xp:old.xp||0,wins:old.wins||0,losses:old.losses||0,melesCount:old.melesCount||0,totalMatches:old.totalMatches||0,lifetimeXP:old.lifetimeXP||0,lifetimeWins:old.lifetimeWins||0,lifetimeLosses:old.lifetimeLosses||0,lifetimeMeles:old.lifetimeMeles||0,highestScore:old.highestScore||0,winRate:old.winRate||0,rank:old.rank||'Unranked',highestRank:old.highestRank||'Unranked',trustScore:old.trustScore??100,emblem:old.emblem||'🛡️',colorHex:old.colorHex||'#FFD700'};await persist('teams',id,t);await loadTeams();renderShell()}
async function updateStats(m,w){for(const id of[m.team1Id,m.team2Id]){const t=state.teams.find(x=>x.teamId===id);if(!t)continue;const won=id===w.winnerId,score=id===m.team1Id?m.team1Score:m.team2Score;t.totalMatches=(t.totalMatches||0)+1;t.gamesPlayed=(t.gamesPlayed||0)+1;t.wins=(t.wins||0)+(won?1:0);t.losses=(t.losses||0)+(won?0:1);t.lifetimeWins=(t.lifetimeWins||0)+(won?1:0);t.lifetimeLosses=(t.lifetimeLosses||0)+(won?0:1);t.melesCount=(t.melesCount||0)+(won&&w.hasMeles?1:0);t.lifetimeMeles=(t.lifetimeMeles||0)+(won&&w.hasMeles?1:0);const gain=won?(w.hasMeles?75:50):10;t.xp=(t.xp||0)+gain;t.seasonXP=(t.seasonXP||0)+gain;t.lifetimeXP=(t.lifetimeXP||0)+gain;t.highestScore=Math.max(t.highestScore||0,score);t.winRate=Math.round((t.wins/t.totalMatches)*100);t.rank=rankFromXp(t.xp);t.highestRank=t.rank;t.lastMatchDate=nowIso();await persist('teams',id,t);await persist('rankings',id,{recordId:id,teamId:id,teamName:t.teamName,xp:t.xp,rank:t.rank,wins:t.wins,losses:t.losses,winRate:t.winRate,trustScore:t.trustScore,updatedAt:nowIso()});for(const pid of[t.player1Id,t.player2Id].filter(Boolean)){const p=state.players.find(x=>x.playerId===pid);if(!p)continue;p.totalMatches=(p.totalMatches||0)+1;p.wins=(p.wins||0)+(won?1:0);p.losses=(p.losses||0)+(won?0:1);p.playerXP=(p.playerXP||0)+gain;p.seasonXP=(p.seasonXP||0)+gain;p.lifetimeXP=(p.lifetimeXP||0)+gain;p.playerLevel=Math.max(1,Math.floor(p.playerXP/250)+1);p.playerRank=rankFromXp(p.playerXP);p.winRate=Math.round((p.wins/p.totalMatches)*100);p.currentWinStreak=won?(p.currentWinStreak||0)+1:0;p.bestWinStreak=Math.max(p.bestWinStreak||0,p.currentWinStreak);p.lastActiveAt=nowIso();p.lastUpdatedAt=nowIso();await persist('players',pid,p)}}await Promise.all([loadTeams(),loadPlayers(),loadRankings()])}
async function completeRound(){const m=state.activeMatch,msg=document.getElementById('roundMessage'),s1=Math.max(0,Number(document.getElementById('round1').value||0)),s2=Math.max(0,Number(document.getElementById('round2').value||0));if(!Number.isFinite(s1)||!Number.isFinite(s2)||(s1===0&&s2===0)){msg.textContent='أدخل نتيجة صحيحة.';return}m.team1Score+=s1;m.team2Score+=s2;m.roundsHistory.push({roundNumber:m.roundNumber,team1RoundScore:s1,team2RoundScore:s2,playedAt:nowIso()});m.roundNumber++;m.lastPlayedTime=nowIso();const w=evaluateWinner(m);if(w){Object.assign(m,{isFinished:true,matchEndDate:nowIso(),winnerTeamId:w.winnerId,winnerTeamName:w.winnerName,winnerTeam:w.winnerName,hasMeles:w.hasMeles,isLocked:true});await updateStats(m,w)}storage.activeMatch=m;await persist('matches',m.matchId,m);await loadMatches();if(w){storage.activeMatch=null;state.activeMatch=null;state.route='history'}renderShell()}
function nav(){return `<nav class="bottom-nav"><div class="bottom-nav-inner"><button class="nav-button ${state.route==='home'?'active':''}" data-route="home"><span>⌂</span>الرئيسية</button><button class="nav-button ${state.route==='players'?'active':''}" data-route="players"><span>●</span>اللاعبون</button><button class="nav-button ${state.route==='teams'?'active':''}" data-route="teams"><span>♜</span>الفرق</button><button class="nav-button ${state.route==='matches'?'active':''}" data-route="matches"><span>◆</span>مباراة</button><button class="nav-button ${state.route==='rankings'?'active':''}" data-route="rankings"><span>★</span>الترتيب</button></div></nav>`}
function renderShell(){app.innerHTML=`${topBar()}<main>${routes[state.route]()}</main>${nav()}`;bindNavigation();bindRouteActions()}
function bindNavigation(){document.querySelectorAll('[data-route]').forEach(el=>el.onclick=()=>{state.route=el.dataset.route;renderShell()})}
function bindRouteActions(){if(state.route==='players'){document.getElementById('savePlayer').onclick=()=>savePlayer().catch(e=>document.getElementById('playerMessage').textContent=e.message);document.querySelectorAll('[data-edit-player]').forEach(b=>b.onclick=()=>{const p=state.players.find(x=>x.playerId===b.dataset.editPlayer);document.getElementById('playerId').value=p.playerId;document.getElementById('playerName').value=p.playerName;document.getElementById('playerBio').value=p.bio||''})}if(state.route==='teams'){document.getElementById('saveTeam').onclick=()=>saveTeam().catch(e=>document.getElementById('teamMessage').textContent=e.message);document.querySelectorAll('[data-edit-team]').forEach(b=>b.onclick=()=>{const t=state.teams.find(x=>x.teamId===b.dataset.editTeam);document.getElementById('teamId').value=t.teamId;document.getElementById('teamName').value=t.teamName||t.name;document.getElementById('player1').value=t.player1Id||'';document.getElementById('player2').value=t.player2Id||''})}if(state.route==='matches'&&!state.activeMatch){const b=document.getElementById('startMatch');if(b)b.onclick=()=>{const t1=document.getElementById('team1').value,t2=document.getElementById('team2').value,msg=document.getElementById('matchMessage');if(!t1||!t2||t1===t2){msg.textContent='اختر فريقين مختلفين.';return}const a=state.teams.find(t=>t.teamId===t1),c=state.teams.find(t=>t.teamId===t2);const m={matchId:guid(),team1Id:t1,team2Id:t2,team1Name:teamName(t1),team2Name:teamName(t2),team1Players:[a?.player1,a?.player2].filter(Boolean).join('، '),team2Players:[c?.player1,c?.player2].filter(Boolean).join('، '),team1Score:0,team2Score:0,team1Player1Id:a?.player1Id||'',team1Player2Id:a?.player2Id||'',team2Player1Id:c?.player1Id||'',team2Player2Id:c?.player2Id||'',roundNumber:1,isLocalRules:document.getElementById('rules').value==='local',matchDate:nowIso(),matchEndDate:null,matchDurationMinutes:0,roundsHistory:[],winnerTeam:'',winnerTeamName:'',winnerTeamId:'',hasMeles:false,isDraw:false,isFinished:false,isLocked:false,lastPlayedTime:nowIso(),displayTitle:`${teamName(t1)} ضد ${teamName(t2)}`,rankedMatch:document.getElementById('ranked').checked,matchVerificationCode:'',isVerified:false,team1Emblem:a?.emblem||'',team2Emblem:c?.emblem||'',team1ColorHex:a?.colorHex||'#FFD700',team2ColorHex:c?.colorHex||'#FFD700'};state.activeMatch=m;storage.activeMatch=m;renderShell()}}if(state.route==='matches'&&state.activeMatch){document.getElementById('addRound').onclick=()=>completeRound().catch(e=>document.getElementById('roundMessage').textContent=e.message);document.getElementById('saveExit').onclick=async()=>{await persist('matches',state.activeMatch.matchId,state.activeMatch);await loadMatches();state.route='home';renderShell()};document.getElementById('cancelMatch').onclick=()=>{if(confirm('إلغاء المباراة الجارية؟')){storage.activeMatch=null;state.activeMatch=null;renderShell()}}}if(state.route==='settings')document.getElementById('logout').onclick=async()=>{try{await api('/api/preview/logout',{method:'POST'})}catch{}storage.session=null;state.session=null;renderAuth()}}
state.session?bootAuthenticated():renderAuth();