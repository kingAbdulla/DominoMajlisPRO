{
  "name": "domino-majlis-pro-cloudflare-preview",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute domino-majlis-preview --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute domino-majlis-preview --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
